{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "crontab",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/crontab",
      "envFile": "${workspaceFolder}/.env"
    }
  ],
  "compounds": [
    {
      "name": "Run All",
      "configurations": ["crontab"],
      "stopAll": true
    }
  ]
}
